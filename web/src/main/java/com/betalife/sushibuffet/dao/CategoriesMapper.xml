<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.betalife.sushibuffet.dao.CategoriesMapper">

	<cache />

	<select id="selectByParentId" parameterType="Categories" resultType="Categories">
		SELECT
			c.id, c.active, c.position, c.thumb, m.name, m.desc
		FROM
			categories c LEFT JOIN multilang m ON m.id = 'c'+c.id  AND m.locale=#{locale}
		WHERE 
			c.parent_id = #{parentId}
	</select>

	<select id="selectAll" parameterType="String"  resultType="Categories">
		SELECT
			c.id, c.active, c.position, c.thumb, m.name, m.desc
		FROM
			categories c LEFT JOIN multilang m ON m.id = 'c'+c.id  AND m.locale=#{locale}
	</select>

</mapper>