<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.betalife.sushibuffet.dao.OrderMapper">

	<resultMap id="orderResult" type="Order">
		<id property="id" column="id" />
		<result property="count" column="count" />
		<result property="created" column="created" />
		<result property="updated" column="updated" />
		<association property="turnover" javaType="Turnover">
			<id property="id" column="turnover_id" />
			<result property="checkout" column="checkout" />
			<result property="tableId" column="table_id" />
		</association>
		<association property="product" javaType="Product">
			<id property="id" column="product_id" />
			<result property="categoryId" column="category_id" />
			<result property="productPrice" column="product_price" />
			<result property="thumb" column="thumb" />
			<result property="productName" column="name" />
			<result property="num" column="num" />
			<result property="productNum" column="product_num" />
		</association>
	</resultMap>

	<select id="selectOrders" parameterType="Order" resultMap="orderResult">
		SELECT o.id, o.`count`, o.turnover_id, o.product_id, t.checkout, o.updated, o.created, p.product_num,
		t.table_id, p.category_id, p.num,
		p.product_price, p.thumb, m.NAME
		FROM
		`order` o, turnover t, product p LEFT JOIN multilang m ON m.id =
		CONCAT('p', p.id) AND m.locale = #{locale}
		WHERE o.turnover_id =
		#{turnover.id}
		AND o.turnover_id = t.id
		AND o.product_id = p.id
	</select>

	<insert id="insertOrder" parameterType="Order" useGeneratedKeys="true"
		keyProperty="id">
		INSERT INTO `order`
		(turnover_id, product_id, `count`)
		VALUES
		(#{turnover.id}, #{product.id}, #{count})
	</insert>

	<update id="updateCount" parameterType="Order">
		update `order`
		set
		`count`=#{count}, updated=now()
		where id=#{id}
	</update>

</mapper>